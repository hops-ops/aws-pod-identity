apiVersion: pkg.crossplane.io/v1
kind: Configuration
metadata:
  name: configuration-aws-pod-identity
spec:
  package: ghcr.io/hops-ops/configuration-aws-pod-identity:{{ .Values.version }}
  packagePullSecrets:
    - name: ghcr
  skipDependencyResolution: {{ .Values.skipDependencyResolution }}

---

# skipDependencyResolution false would rename this function; keep explicit name to match package path.
apiVersion: pkg.crossplane.io/v1
kind: Function
metadata:
  name: hops-ops-configuration-aws-pod-identityrender
spec:
  package: ghcr.io/hops-ops/configuration-aws-pod-identity_render:{{ .Values.version }}
  packagePullSecrets:
    - name: ghcr
  skipDependencyResolution: {{ .Values.skipDependencyResolution }}
