apiVersion: pkg.crossplane.io/v1
kind: Configuration
metadata:
  name: configuration-aws-eks-pod-identity
spec:
  package: ghcr.io/hops-ops/configuration-aws-eks-pod-identity:{{ .Values.version }}
  packagePullSecrets:
    - name: ghcr
  skipDependencyResolution: {{ .Values.skipDependencyResolution }}

---

# skipDependencyResolution false would rename this function; keep explicit name to match package path.
apiVersion: pkg.crossplane.io/v1
kind: Function
metadata:
  name: hops-ops-configuration-aws-eks-pod-identityrender
spec:
  package: ghcr.io/hops-ops/configuration-aws-eks-pod-identity_render:{{ .Values.version }}
  packagePullSecrets:
    - name: ghcr
  skipDependencyResolution: {{ .Values.skipDependencyResolution }}
